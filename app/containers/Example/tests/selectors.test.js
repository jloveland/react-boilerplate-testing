// import { fromJS } from 'immutable';
// import { makeSelectExampleDomain } from '../selectors';

// const selector = makeSelectExampleDomain();

describe('makeSelectExampleDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
